---
title: "43_Full_Model"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
source("00_requirements.R")
source("01_plot_functions.R")
load("../data/42_NBA-Analysis.RData")
```


``` {r}
full_model <- nba_processed %>%
  mutate(Win = as.numeric(`Win/Loss Status` == "W")) %>%  
  glm(
    Win ~ `Three Point Volume` + `Three Pointer Accuracy` + `Two Pointer Accuracy` + `Free Throw Accuracy` + `Assists` + `Offensive Rebounds` + `Turnover` + `Personal Fouls Drawn` + `Blocks Against` + `Defensive Rebounds` + `Steals` + `Personal Fouls` + `Blocks`,
    data = .,
    family = binomial(link = "logit")
  )

summary(offense_model)
```

``` {r}
save.image("../data/43_NBA-Analysis.RData")
```